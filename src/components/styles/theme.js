import colors from './colors';
import padding from './padding';
import typography from './typography';

const borderRadius = '4px';

const theme = {
  borderRadius,
  colors,
  padding,
  typography,
};

export default theme;
export { borderRadius, colors, padding, typography };
